#include <stdio.h>

int main()
{
    // dataType variableName = variableValue
    int age = 21;

    // variable naming rules
    /**
     * 1. Variable name can only contain a-z, A-Z, 0-9 & _
     * 2. Variable names can't contain spaces in between
     * 3. Variable names can't start with number/digits
     * 4. Key words can't be used as variable name
     */

    return 0;
}